Terraform example for Ud-Dolphin

Usage

Initialize and preview the plan:

```bash
terraform init
terraform plan
```

Apply (creates an AWS VPC):

```bash
terraform apply
```

Variables can be overridden via `terraform.tfvars` or the CLI.
